services:
  generator:
    build:
      context: .
      target: generator
    depends_on:
      - server
  
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: server
    ports:
      - 8080:8080
